-- 🎯 PURE SILENT AIM (Q-LOCK) - MIT HUB INTEGRATION
-- Drück Q -> Aimlock auf Maus-Target.

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local Workspace = game:GetService("Workspace")

local LocalPlayer = Players.LocalPlayer
local Camera = Workspace.CurrentCamera
local Mouse = LocalPlayer:GetMouse()

-- === HUB VERBINDUNG (wie AutoStomp) ===
local Shared = getgenv().SchmecktHubPC or {}
getgenv().SchmecktHubPC = Shared

local key = "SilentAimQMethod"

-- Alte Connections cleanen
Shared.Connections = Shared.Connections or {}
if Shared.Connections[key] then
    for _, conn in pairs(Shared.Connections[key]) do
        pcall(function() conn:Disconnect() end)
    end
end
Shared.Connections[key] = {}

-- Alte GUI/Highlight cleanen
if Shared._QMethodGUI then pcall(function() Shared._QMethodGUI:Destroy() end) end
if Shared._QMethodHL then pcall(function() Shared._QMethodHL:Destroy() end) end

-- === STATUS ===
local isLocked = false
local targetPlr = nil
local targetPart = nil
local predictionPos = nil
local Data = {}

-- === GUI ===
local gui = Instance.new("ScreenGui")
gui.Name = "SilentStatusQ"
gui.ResetOnSpawn = false
gui.Parent = LocalPlayer:WaitForChild("PlayerGui")
Shared._QMethodGUI = gui

local label = Instance.new("TextLabel")
label.Size = UDim2.new(0, 200, 0, 40)
label.Position = UDim2.new(0.5, -100, 0.85, 0)
label.BackgroundTransparency = 1
label.Text = ""
label.Font = Enum.Font.GothamBlack
label.TextSize = 20
label.TextStrokeTransparency = 0.5
label.Parent = gui

-- === HIGHLIGHT ===
local hl = Instance.new("Highlight")
hl.Name = "SilentHLQ"
hl.FillColor = Color3.fromRGB(255, 0, 0)
hl.FillTransparency = 0.5
hl.OutlineColor = Color3.fromRGB(255, 255, 255)
hl.OutlineTransparency = 0
hl.Enabled = false
hl.Parent = game:GetService("CoreGui")
Shared._QMethodHL = hl

-- === FUNKTIONEN ===

local function getClosestToMouse()
    local bestDist = math.huge
    local bestTarget = nil
    local bestPart = nil
    local mp = UserInputService:GetMouseLocation()

    for _, plr in ipairs(Players:GetPlayers()) do
        if plr ~= LocalPlayer and plr.Character then
            local char = plr.Character
            local hum = char:FindFirstChild("Humanoid")
            local head = char:FindFirstChild("Head")
            local hrp = char:FindFirstChild("HumanoidRootPart")
            
            if hum and hum.Health > 0 and head and hrp then
                local params = RaycastParams.new()
                params.FilterType = Enum.RaycastFilterType.Exclude
                params.FilterDescendantsInstances = {LocalPlayer.Character, char}
                local ray = Workspace:Raycast(Camera.CFrame.Position, (head.Position - Camera.CFrame.Position), params)
                
                if not ray then
                    local pos, onScreen = Camera:WorldToViewportPoint(head.Position)
                    if onScreen then
                        local dist = (Vector2.new(pos.X, pos.Y) - mp).Magnitude
                        if dist < bestDist then
                            bestDist = dist
                            bestTarget = char
                            bestPart = head
                        end
                    end
                end
            end
        end
    end
    return bestTarget, bestPart
end

local function getPrediction(char, part)
    if not char then return nil end
    local hrp = char:FindFirstChild("HumanoidRootPart")
    if not hrp then return part.Position end

    local vel = hrp.AssemblyLinearVelocity or Vector3.zero
    
    local ping = 0.06
    pcall(function() ping = LocalPlayer:GetNetworkPing() end)
    
    local dist = (part.Position - Camera.CFrame.Position).Magnitude
    local timeToHit = (dist / 1000) + ping
    
    return part.Position + (vel * timeToHit)
end

local function unlock()
    isLocked = false
    targetPlr = nil
    targetPart = nil
    predictionPos = nil
    hl.Enabled = false
    label.Text = ""
end

local function toggleLock()
    -- Prüfe ob Hub es erlaubt: SilentAim AN und Methode ist QMethod
    if not (Shared.SilentAim and Shared.SilentAimMethod == "QMethod") then
        unlock()
        return
    end
    
    if isLocked then
        unlock()
    else
        local t, p = getClosestToMouse()
        if t and p then
            isLocked = true
            targetPlr = t
            targetPart = p
            hl.Adornee = t
            hl.Enabled = true
            label.Text = "LOCKED: " .. t.Name
            label.TextColor3 = Color3.fromRGB(255, 50, 50)
        end
    end
end

-- === MAIN LOOP (wie AutoStomp mit Shared.Connections) ===
table.insert(Shared.Connections[key], RunService.RenderStepped:Connect(function()
    -- Prüfe Hub Status
    if not (Shared.SilentAim and Shared.SilentAimMethod == "QMethod") then
        if isLocked then unlock() end
        gui.Enabled = false
        hl.Enabled = false
        return
    end
    
    gui.Enabled = true
    
    if isLocked and targetPlr and targetPlr.Parent then
        local hum = targetPlr:FindFirstChild("Humanoid")
        if not hum or hum.Health <= 0 then
            unlock()
            return
        end
        predictionPos = getPrediction(targetPlr, targetPart)
    else
        if isLocked then unlock() end
    end
end))

-- === INPUT ===
table.insert(Shared.Connections[key], UserInputService.InputBegan:Connect(function(input, gp)
    if gp then return end
    if input.KeyCode == Enum.KeyCode.Q then
        if Shared.SilentAim and Shared.SilentAimMethod == "QMethod" then
            toggleLock()
        end
    end
end))

-- === HOOKS ===
local mt = getrawmetatable(game)
local oldNc = mt.__namecall
local oldIdx = mt.__index
setreadonly(mt, false)

mt.__namecall = newcclosure(function(self, ...)
    local m = getnamecallmethod()
    local a = {...}
    
    -- Nur wenn Hub es erlaubt UND gelocked
    if Shared.SilentAim and Shared.SilentAimMethod == "QMethod" and isLocked and predictionPos then
        if m == "Raycast" and self == Workspace then
            if a[1] and a[2] then
                a[2] = (predictionPos - a[1]).Unit * a[2].Magnitude
                return oldNc(self, unpack(a))
            end
        end
        if self == Mouse and (m == "GetHit" or m == "Hit") then
            return CFrame.new(predictionPos)
        end
    end
    return oldNc(self, ...)
end)

mt.__index = newcclosure(function(self, k)
    if Shared.SilentAim and Shared.SilentAimMethod == "QMethod" and isLocked and predictionPos and self == Mouse then
        if k == "Hit" then return CFrame.new(predictionPos) end
        if k == "Target" then return targetPart end
    end
    return oldIdx(self, k)
end)

setreadonly(mt, true)
